import { className } from "@/types/style.types";

export const styles: className = {
    sidebar_header: " dark:bg-neutral-900 flex flex-row items-center",
    sidebar_header_label: "font-bold text-lg text-nowrap",
    sidebar_menu_link_item: "font-semibold cursor-default text-nowrap"
}